export const Style = Object.freeze({
  BRAND: 'brand',
  DARK: 'dark',
  LIGHT: 'light',
})

export const Type = Object.freeze({
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
})

export const LinkType = Object.freeze({
  DEFAULT: 'default',
  UNDERLINE: 'underline',
})
